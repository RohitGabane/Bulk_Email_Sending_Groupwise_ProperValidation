@* 
 @model Bulk_Email_Sending_Groupwise.Models.Department

@{
    ViewData["Title"] = "Create";
}

<div class="container mt-5">
    <h1>Create Department</h1>

    <div class="row">
        <div class="col-md-6">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="Dept_Name" class="control-label">Department Name</label>
                    <input asp-for="Dept_Name" class="form-control" />
                    <span asp-validation-for="Dept_Name" class="text-danger"></span>
                </div>
                <br/>
                <div class="form-group">
                    <button type="submit" onclick="onsuccessclick()" class="btn btn-primary">Create</button>
                    <a asp-action="Index" class="btn btn-secondary">Back to List</a>

                </div>
            </form>
        </div>
    </div>

    <div class="mt-3">
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
<script>
    function onsuccessclick() {
        alert("Create successfully");
    }
</script> *@
@model Bulk_Email_Sending_Groupwise.Models.Department

@{
    ViewData["Title"] = "Create";
}

<div class="container mt-5">
    <h1 class="text-center">Create Department</h1>
    <hr/>
    <div class="row">
        <div class="col-md-6 offset-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="Dept_Name" class="control-label">Department Name</label>
                    <input asp-for="Dept_Name" class="form-control" maxlength="20" required/>
                    <span asp-validation-for="Dept_Name" class="text-danger"></span>
                </div>
                <br/>
                <div class="form-group">
                    <button type="submit" onclick="onsuccessclick()" class="btn btn-primary">Create</button>
                    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </div>

    <div class="mt-3">
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
<script>
    function onsuccessclick() {
        alert("Department created successfully");
    }
</script>
