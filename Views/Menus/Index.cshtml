@* @model IEnumerable<Bulk_Email_Sending_Groupwise.Models.Menus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MenuName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParentID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MenuName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MenuID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MenuID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MenuID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@
 @model IEnumerable<Bulk_Email_Sending_Groupwise.Models.Menus>

@{
    ViewData["Title"] = "Menu Index";
}

<div class="container mt-5">
    <h1>Menu Index</h1>

    <p>
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </p>

    <table class="table table-striped">
        <thead class="thead-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.MenuName)</th>
                <th>@Html.DisplayNameFor(model => model.ParentID)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.MenuName)</td>
                    <td>@Html.DisplayFor(modelItem => item.ParentID)</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.MenuID" class="btn btn-warning">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.MenuID" class="btn btn-success">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.MenuID" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
